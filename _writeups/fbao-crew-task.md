### Front-end/gameplay batch: queue item 60, part 3 — a real crew-directable FBAO-clearance task, reusing the player's own resolution mechanism rather than duplicating it

Confirmed the gap by reading the tree before building anything (lesson 16), per CLAUDE.md section 6's item 60 own filing: parts 1 (nebulized epinephrine) and 2 (tracheostomy state) are done/another agent's assignment respectively; part 3 (the FBAO-crew-task sub-gap) was still open. Grepped `s.cleared`/`conditionHas(...,"fbao")` across `App.jsx` before touching anything: the mechanism was reachable only through the player's own two hard-coded special cases inside `start()` (`p.id==="cpr"` and `p.id==="laryngoscopy"`, both gated on `conditionHas(scenOf(s).condition,"fbao")&&!s.cleared&&!s.pushedDeeper`) plus `fbao`'s own scenario-local `clearFB` Magill-forceps extra (`scenarios.js`). No `TASKS` entry in `gear.js` let a crew member be directed to perform this at all — confirmed by reading every existing `TASKS` entry, none references `cleared`/`pushedDeeper`/`fbao`. This meant a directed crew "Compressions" task (`t.dose:"cpr"`, already `lvl:0`) doses real CPR but never clears a foreign-body airway, since `crewFn` (the function that resolves crew-directed tasks) is a completely separate code path from `start()`'s player-action resolution and never ran that special case.

**Fixed by calling the SAME resolution the player's own action already uses, not by building a second, parallel clearance rule.** Added a new branch to `crewFn` in `App.jsx`, gated on two new task flags (`t.fbaoClear`/`t.fbaoMagill`), that runs the identical `conditionHas(scenOf(m).condition,"fbao")&&!m.cleared&&!m.pushedDeeper` guard the player's own `cpr`/`laryngoscopy` special cases already use, and sets `m.cleared=1` on success — the exact same state field, the exact same guard conditions, no separate ledger. Two negative branches were added alongside it, matching the honesty standard the rest of this crew-direction file already holds to: a crew member directed at this task on a patient whose airway was already pushed deeper (the fingerSweep extra) gets a real refusal ("wedged, I can't reach it"), not a silent success; a crew member directed at this task on a patient who does NOT have a real foreign-body obstruction gets an honest "there's nothing obstructing this airway" refusal rather than silently clearing an airway that was never blocked (a specificity guarantee, matching the mechanismWiring suite's own standing "does not fire in patients who do not have it" discipline, even though this is a front-end-only mechanism with no physiology-suite consumer).

**Two new `gear.js` TASKS entries, BLS and ALS tiers, per real scope-of-practice, not a single generic entry:**
- `fbaoClearBls` (`lvl:0`, `fbaoClear:true`, `dose:"cpr"`) — real 2020 AHA/NREMT guidance for an unconscious complete FBAO is chest compressions THEMSELVES (they generate the intrathoracic pressure spike that dislodges the bolus), not back blows/abdominal thrusts, which only apply to a conscious, standing patient — the same clinical point `fbao`'s own scenario `resolve()` text already teaches the player. Doses real CPR via `dose:"cpr"` in addition to clearing the airway, so a directed compressor genuinely gets both consequences, matching what the player's own `p.id==="cpr"` special case already does.
- `fbaoMagillClear` (`lvl:4`, `fbaoMagill:true`) — direct laryngoscopy + Magill forceps under direct visualization, the definitive ALS-scope removal, matching the exact scope tier (`bag:"airway"`, `lvl:4`) `fbao`'s own scenario-local `clearFB` player extra already requires.

**MEASURED, not assumed** (a throwaway probe, stripped after use, mirroring `crewFn`'s exact new branch logic against the real `TASKS`/`SCEN` exports): both new tasks exist with the correct fields; both correctly set `cleared=1` against the real `fbao` scenario's own condition key; both correctly refuse (no clearance, `cleared` stays 0) against a non-fbao condition (`abdPain`) and against a pushed-deeper airway; and `choking40` — which reuses the SAME `fbao` condition key per an earlier session's own fix — is also correctly reachable through the new BLS task, closing the same "no way to ever clear this airway" gap that scenario had before that earlier fix, now also true for a crew-directed order rather than only the player's own hands.

**Verification.** `npx eslint src/App.jsx src/gear.js`: exactly the pre-existing 3-error `react-refresh/only-export-components` baseline in `App.jsx`, zero findings in `gear.js`. `npx vite build`: clean (21.65s, same pre-existing >500kB chunk-size warning). No new physiology field was introduced (this reuses the existing session-scoped `s.cleared`/`s.pushedDeeper` flags `fbao`'s own condition already defines), so no `scenarioSweep.mjs` REQUIRED/NON_NEGATIVE additions were needed, and — per this document's own established precedent for App.jsx/gear.js-only, non-`src/physio/*` changes (e.g. the F14/F15/protocol-content-only batches already on record) — no `mechanismWiring.mjs` assertion was added either, since that suite instantiates `physio()`/`Patient` directly and does not exercise `App.jsx`'s crew-direction UI layer at all; the throwaway probe above is this batch's own real, measured evidence. The probe script was stripped before this entry was written, confirmed via a directory listing showing no `_tmp_*` files remain under `src/scripts/`.

### 6. Resolution note

RESOLVED (this session) — see section 3's newest entry. `gear.js` gained `fbaoClearBls`/`fbaoMagillClear` (real BLS/ALS-tier crew-directable FBAO-clearance tasks), and `App.jsx`'s `crewFn` gained a branch calling the SAME `conditionHas(...,"fbao")`/`s.cleared`/`s.pushedDeeper` resolution the player's own `cpr`/`laryngoscopy` special cases already use. The FBAO-crew-task sub-gap of queue item 60 is closed; the tracheostomy-state sub-gap was being worked by a concurrent agent this session and its status should be re-checked against the tree before assuming item 60 is fully closed.
