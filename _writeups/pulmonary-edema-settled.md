### Investigation only, no changes made: item 7 standing workstream's "pulmonary edema separated from generic CHF" suggestion was already investigated, built, measured, and deliberately reverted — a real negative result, respected rather than re-attempted

Assigned as item 3 of this session's four-item batch. Per the collision protocol's own instruction, and per this session's own explicit task briefing ("if a prior session found a real reason NOT to split it... respect that finding and move to item 4 instead"), confirmed via `grep -n "pulmonary edema" CLAUDE.md`: a prior session already built a real, Starling-based `updatePulmonaryEdemaFormation(pat, dt, Ppv)` mechanism in `cardiovascular.js`, derived `chf`/`ami`'s edema from the solver's own `Ppv` (pulmonary venous pressure) instead of a direct stat-write ratchet, iterated calibration through three thresholds, and then found a disqualifying cross-cutting confound on a fifth check (the one CLAUDE.md's own lesson explicitly says should have been run first): `opioidOD` (pure respiratory depression, no cardiac lesion) produced a HIGHER measured Ppv (10.61 mean) than `chf` itself (10.1-10.2), because `Ppv` in this solver is confounded by intrathoracic-pressure swings from labored/absent breathing and assisted ventilation — a real mechanical effect indistinguishable from cardiogenic congestion by threshold alone. This regressed three previously-robust, unrelated BVM/assisted-ventilation `mechanismWiring.mjs` assertions when live.

The prior session's own documented decision: revert, not force-fix — "no threshold on raw Ppv can separate real cardiac filling-pressure elevation from apnea/PPV-driven intrathoracic-pressure artifact when the latter's signal is as large or larger." The new mechanism and `pcwp` derivation change were fully removed; `chf`/`ami` were restored to their exact original direct-write form, with the reasoning recorded in-code so a future session doesn't silently redo the same experiment blind. The document explicitly names what a future attempt should do differently (measure Ppv, or whatever replaces it, against a non-cardiac respiratory-depression control from the FIRST calibration pass, and likely correct Ppv for `pat.intrathoracicP`/`extP` before using it as a Starling driver, or find a genuinely separate left-heart-filling-pressure state).

This is real, useful, already-documented information, not an unbuilt gap — respected per this session's own instruction rather than re-attempted. No code changes were made for this item; time was redirected to the session's other three assigned items.

### 6. Resolution note

SETTLED (a prior session), not reopened this session — see CLAUDE.md section 3's "pulmonary edema separated from generic CHF... INVESTIGATED, BUILT, MEASURED, and REVERTED" entry for the full mechanism, measurement, and the specific pitfall a future attempt should avoid (test against a non-cardiac respiratory-depression control, e.g. `opioidOD`, from the first calibration pass). This session confirmed the finding still holds and deliberately did not re-attempt it, per explicit instruction.
